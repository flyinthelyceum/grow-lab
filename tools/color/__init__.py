"""Colour-measurement tooling for the bench.

Not part of the station runtime — `pyproject.toml` packages `pi*` only. These
modules run from a clone, the same way `cad/` does, and depend on nothing
outside the standard library so the workflow survives a machine with no venv.
"""
